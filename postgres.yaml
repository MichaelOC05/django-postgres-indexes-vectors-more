services:
  db:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_PASSWORD: ${PASSWORD}
      POSTGRES_DB: testing-database
    ports:
      - "5432:5432"
    shm_size: 128mb